#!/bin/rc

sysctl -p /etc/sysctl.conf

hostname oasis &
ip link set lo up &
chmod 666 /dev/fuse &
if([ -d /dev/dri ]) {
	chmod 660 /dev/dri/*
	chown root:video /dev/dri/*
} &
if([ -d /dev/snd ]) {
	chmod 660 /dev/snd/*
	chown root:audio /dev/snd/*
} &

wait
